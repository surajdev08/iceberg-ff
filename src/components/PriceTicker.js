export default function PriceTicker() {
  return (
    <section className="price-ticker-strip">
      <div className="price-ticker-track" id="price-ticker-track">
        {/* JS dynamically populates rolling currency cards */}
      </div>
    </section>
  );
}
